"""
Tests for the benchmark API.

We'll expand these later. For now, just basic structure.

To run tests:
    pytest tests/

Key concepts:
    - Fixtures: Reusable setup code (like creating a test database)
    - Mocking: Replace real API calls with fake ones (so tests don't cost money)
"""

import pytest


def test_placeholder():
    """
    Placeholder test to verify pytest is working.

    We'll add real tests once we verify the app runs.
    """
    assert 1 + 1 == 2
